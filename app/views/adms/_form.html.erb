<%= form_with(model: adm, local: true) do |form| %>
  <% if adm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adm.errors.count, "error") %> prohibited this adm from being saved:</h2>

      <ul>
      <% adm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
